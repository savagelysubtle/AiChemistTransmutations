# Gumroad Webhook Server Dependencies
# Install these on your deployment server (Railway, Render, AWS Lambda)
# Installation: pip install -r requirements-webhook.txt

# Web framework
flask>=3.0.0
gunicorn>=21.2.0

# Supabase client
supabase>=2.0.0

# Cryptography for license signing
cryptography>=41.0.0

# YAML parsing for configuration
pyyaml>=6.0.1

# HTTP requests (for validation script)
requests>=2.31.0

# Optional: Production monitoring
sentry-sdk[flask]>=1.39.0

# Optional: Structured logging
python-json-logger>=2.0.7

